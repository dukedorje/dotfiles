(( $+commands[brew] )) && FZF_DIR=$(brew --prefix fzf)
[[ -d "$FZF_DIR" ]] && source "$FZF_DIR/shell/key-bindings.zsh"
[[ -d "$FZF_DIR" ]] && source "$FZF_DIR/shell/completion.zsh"

export FZF_DEFAULT_COMMAND='fd --type f --hidden --follow --exclude .git'
#export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
